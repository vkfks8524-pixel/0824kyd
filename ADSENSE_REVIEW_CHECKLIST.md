# KYD AdSense 재심사 점검 기록

수정 작업: 2026-09-06~07. 이 문서는 승인 보증이나 Google의 심사 결과가 아니다.

## 확인된 사실과 추정 구분

- 사용자가 제공한 2026-09-02 화면의 사유는 **가치가 별로 없는 콘텐츠**이며 ads.txt 상태는 **승인됨**이다. ads.txt 해결과 사이트 승인은 별개다.
- 실제 HTTP 점검에서 /blog는 404, /public/과 /public/posts/windows-file-extension/은 정상 본문을 200으로 반환했다. 중복 경로는 개선 대상이지만 Google이 이것 때문에 거절했다고 단정할 수 없다.
- 기존 글에는 본문 중간의 요약 뒤에 비슷한 점검 순서가 다시 등장한다. 링크·글자 수·JSON-LD 검사가 통과해도 독창성이나 유용성을 입증하지 못한다.
- 공식 안내는 고유 콘텐츠, 방문자에게 제공하는 가치, 탐색 편의성을 요구한다: https://support.google.com/adsense/answer/7299563?hl=ko

## 이번 수정 범위

- 확장자·PDF 링크·브라우저 캐시·저장공간·Cloudflare Pages **5개 글의 본문을 재구성**했다. 비교 입력, 판단 한계, 결과별 다음 행동을 보강했다.
- 확장자로 실제 파일 내용을 확정하는 표현을 바로잡고 Windows 10·11 메뉴를 구분했다. 저장공간 목표는 설명용 계산임을 명시했다.
- KYD에서 실제 관찰한 중복 경로 문제를 운영 사례로 기록했다. 직접 하지 않은 기기 실험·경험·성과는 만들지 않았다.
- 다른 17개 글은 중간의 반복 요약만 정리했다. 이 편집을 23개 전체의 전면 품질 검토로 간주하지 않는다. 스미싱 글은 이번에 재작성하지 않았다.
- URL의 @ 위치 오판, 지원하지 않는 스킴, 파일명 끝의 점, 양방향 표시 문자, 오래된 Office 파일 주의 안내를 수정했다.
- 공개 예시 버튼과 tools/test-cases.json, scripts/test-tools.cjs를 추가했다.
- /public 및 하위 경로를 원래 주소로, /blog 및 /blog/를 /posts/로 연결하는 301 규칙을 추가했다. 글 주소 자체를 옮기지는 않았다.
- sitemap의 일괄 고정 날짜를 없애고 글의 실제 수정 메타데이터를 사용한다. 비기사 페이지에는 확인하지 않은 날짜를 넣지 않는다.
- 글자 수 하한 경고를 제거했다. 글자 수나 게시물 수를 승인 요건처럼 사용하지 않는다.

## 검증과 재현

PowerShell 7에서 scripts/generate-sitemap.ps1, scripts/validate-site.ps1, scripts/check-external-links.ps1을 실행한다. Playwright 모듈을 사용할 수 있는 Node 환경에서 node scripts/test-tools.cjs를 실행한다.

- 브라우저 동작 시험: URL 11개, 파일명 10개, 계산 5개, 계정 선택·초기화 4개 = 30개. 추가로 예시 버튼 동작을 검사한다.
- 11개 페이지에서 390px/1280px 가로 넘침과 페이지 내 앵커를 검사한다. 파일 확장자 글의 모바일·PC 스크린샷을 생성한다.
- 자동 시험에서는 외부 요청을 차단해 광고 노출·클릭이 발생하지 않도록 한다. 실제 휴대전화나 모든 운영체제의 검증을 대신하지 않는다.
- 루트와 public/의 배포 파일을 동일하게 유지한다.
- 배포 후에는 35개 canonical 주소, ads.txt, robots.txt, sitemap.xml, /blog, /public/, 하위 글 리디렉션, 존재하지 않는 주소의 실제 404를 확인한다.

## 재심사 전 남은 판단

1. 나머지 18개 글은 필요한 경우 유사 주제를 합치거나 구체적 비교·실제 사례 중심으로 추가 개정한다. 이번 5개 개정으로 전체 사이트의 가치 문제가 해소됐다고 보장할 수 없다.
2. 운영자가 제공할 수 있는 실제 이용 사례·직접 촬영한 화면·측정 결과를 확인해 추가한다. 해당 자료를 승인 필수 조건이라고 주장하거나 없는 자료를 만들지 않는다.
3. AdSense 화면에서 현재 세부 사유, 재심사 가능 여부, 코드 및 ads.txt 상태를 다시 확인한다. 9월 2일 화면 이후 계정 상태를 확인한 것으로 적지 않는다.
4. 개인정보 보호 메시지/CMP와 자동 광고 제외는 현재 계정 설정을 확인해야 한다. 기존 8월 기록을 현재 상태의 증거로 쓰지 않는다.
5. Search Console의 실제 색인·크롤링 결과를 확인한다. 사이트맵 제출·기술 검사 통과 자체는 승인 근거가 아니다.
6. 변경 반영과 내용 검토 없이 같은 상태로 재신청하지 않는다. 임의의 대기 일수나 승인 확률을 제시하지 않는다. 이번 작업에서 재심사를 자동 요청하지 않는다.

승인 후에도 본문을 없애고 단순 링크 모음으로 바꾸는 경우는 별도 검토가 필요하다. 광고를 메뉴·링크·다운로드 버튼처럼 보이게 배치하지 않는다.
